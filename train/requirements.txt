torch
# Phi-4-mini isn't in every transformers release's native model mapping, so
# it's loaded with trust_remote_code=True (the repo's bundled modeling
# file). hf_utils.py patches the transformers-version skew that file hits
# (a removed/renamed `LossKwargs` symbol) rather than pinning an exact
# transformers version here.
transformers==4.49.0
accelerate==1.3.0
peft>=0.13
trl>=0.11
bitsandbytes>=0.44
accelerate>=1.0
datasets
huggingface_hub>=0.24
hf_transfer
